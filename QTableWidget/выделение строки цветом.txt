		if(currentRow != previousRow)
		{
			if(previousRow >= 0)
			{
				for (int col = 0; col < table.ptr->columnCount(); ++col) {
					QTableWidgetItem* item = table.ptr->item(previousRow, col);
					if (!item)
					{
						item = new QTableWidgetItem();
						table.ptr->setItem(previousRow, col, item);
					}
					item->setBackground(Qt::NoBrush);  // Сброс цвета фона
				}
			}

			if(currentRow >= 0)
			{
				for (int col = 0; col < table.ptr->columnCount(); ++col) {
					QTableWidgetItem* item = table.ptr->item(currentRow, col);
					if (!item)
					{
						item = new QTableWidgetItem();
						table.ptr->setItem(previousRow, col, item);
					}
					item->setBackground(Qt::yellow);  // Сброс цвета фона
				}
			}
		}